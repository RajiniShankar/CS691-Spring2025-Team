import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["upload.wikimedia.org", "cdn.worldvectorlogo", "img.freepik.com"],
  }
};

export default nextConfig;
